name: CI - Ruff

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-22.04
    steps:
      - name: Check code
        uses: actions/checkout@v4

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install lib
        run: |
          pip install ruff

      - name: Run Ruff Check
        run: |
          ruff check . --exclude=README.md

      - name: Run Ruff Format
        run: |
          ruff format